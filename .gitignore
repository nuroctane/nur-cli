# Build
/target
**/*.rs.bk
*.pdb
.DS_Store

# Graphify knowledge graph (regenerate: graphify extract . --code-only)
graphify-out/

# Secrets & local agent state (NEVER commit)
.env
.env.*
.envrc
*.pem
*.key
*.p12
auth.json
**/auth.json
# Project-local agent state (use .nur — never .muse)
.nur/
**/.nur/
.muse/
**/.muse/
credentials*
**/credentials*
secrets*
**/secrets*

# Local agent instructions only (never publish to main)
AGENTS.md

# Editor / OS
.idea/
.vscode/
*.swp
*~
Thumbs.db

.ref-frames/

# MkDocs build output
site/

# foglamp codebase scan — scan.lock.json holds a secret edit token
.foglamp/


# Python bytecode (session_reader.py syntax checks)
__pycache__/
*.pyc
