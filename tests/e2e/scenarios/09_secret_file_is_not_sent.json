{
  "skip": "open decision: nur sends file contents the model reads verbatim (as Claude Code and Codex do); docs/security.md promises no redaction. Remove this key if tool results should be secret-redacted before they leave the machine.",
  "description": "A secret in a .env file read by the model is not sent verbatim to the provider.",
  "prompt": "Read the env file.",
  "setup_files": {
    ".env": "OPENAI_API_KEY=sk-e2etestsecret0123456789abcdefghijklmnop\n"
  },
  "script": [
    {
      "tool_calls": [
        {
          "name": "read_file",
          "arguments": {
            "path": ".env"
          }
        }
      ]
    },
    {
      "text": "Read it."
    }
  ],
  "expect": {
    "requests_lack": [
      "sk-e2etestsecret0123456789abcdefghijklmnop"
    ]
  }
}
